{
  "name": "budget-app",
  "version": "1.0.0",
  "description": "An app that allows users to actively create transactions while offline",
  "main": "server.js",
  "scripts": {
    "start": "if-env NODE_ENV=production && npm run start:prod || npm run start:dev",
    "start:prod": "npm run build && node server.js",
    "start:dev": "concurrently \"npm run server\" \"cd client && npm run start\"",
    "server": "nodemon server.js --prefix client",
    "build": "cd client && npm run build",
    "install": "cd client && npm install",
    "client": "cd client && npm start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "compression": "^1.7.4",
    "mongoose": "^5.5.15",
    "if-env": "^1.0.4",
    "morgan": "^1.9.1"
  },
  "devDependencies": {
    "concurrently": "^5.2.0",
    "nodemon": "^2.0.4"
  }
}
